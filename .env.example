# Chicago SMB Market Radar - Environment Configuration Template
# 
# SECURITY NOTICE: This is a template file. Copy to .env and fill with actual values.
# NEVER commit .env files to version control!
#
# Instructions:
# 1. Copy this file: cp .env.example .env  
# 2. Fill in your actual credentials below
# 3. Ensure .env is in .gitignore (it should be!)

# Google Service Account Configuration
# Path to your Google Cloud service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Google Sheets Configuration  
# The ID of your Google Sheet (from the URL)
SHEET_ID=your_sheet_id_here

# Worksheet Tab Names
TAB_LICENSES=licenses_weekly
TAB_PERMITS=permits_weekly  
TAB_CTA=cta_weekly
TAB_SUMMARY=summary_latest

# Data Processing Configuration
DAYS_LOOKBACK=90
WEEKLY_BASELINE_WEEKS=13

# Feature Flags
ENABLE_PERMITS=true
ENABLE_CTA=true

# Security Configuration (Optional - Advanced)
# Encryption key for local data storage (generate with: openssl rand -hex 32)
DATA_ENCRYPTION_KEY=your_32_byte_hex_key_here

# Rate Limiting (requests per second)
API_RATE_LIMIT=1.0

# Logging Configuration
LOG_LEVEL=INFO
SECURITY_LOG_ENABLED=true